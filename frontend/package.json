{
  "name": "chatbot-pdf-deepseek-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:frontend": "cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "dev": "cd frontend && npm run dev",
    "preview": "cd frontend && npm run preview"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "vite": "^6.3.3"
  }
}
